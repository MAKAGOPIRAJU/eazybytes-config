server:
  port: 8072

# I want to connect with the eureka server since i am the only single entry point i should aware of all the microservices first
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"
